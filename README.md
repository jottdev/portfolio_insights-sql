# portfolio_insights-sql
Loan SQL Schema
A small PostgreSQL schema for managing clients, loan applications, loans, and payments.
Includes basic validation, relationships, and safe DROP TABLE IF EXISTS rebuilds.